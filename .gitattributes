# This repo is LF-everywhere (848 of 849 tracked text files).
# Declaring it explicitly stops core.autocrlf=true flagging unchanged
# files as modified on Windows.
* text=auto eol=lf

# Binary — never line-ending convert or diff as text
*.xlsx binary
*.png  binary
*.jpg  binary
*.pdf  binary
